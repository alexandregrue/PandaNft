<form method="post" action="{{ path('app_road_map_delete', {'id': road_map.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ road_map.id) }}">
    <button class="btn">Delete</button>
</form>
