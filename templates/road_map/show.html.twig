{% extends 'base.html.twig' %}

{% block title %}RoadMap{% endblock %}

{% block body %}
    <h1>RoadMap</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ road_map.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ road_map.title }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ road_map.date }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ road_map.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_road_map_index') }}">back to list</a>

    <a href="{{ path('app_road_map_edit', {'id': road_map.id}) }}">edit</a>

    {{ include('road_map/_delete_form.html.twig') }}
{% endblock %}
